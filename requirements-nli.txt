# Optional. Only needed for `--backend nli`.
#
# The rest of this repo runs on the Python standard library with nothing
# installed, and that is deliberate: a reviewer clones it and runs one command.
# These pull in roughly 1 GB of wheels plus a further ~700 MB of model weights
# on first use, which is why the NLI comparator is a backend you switch on
# rather than the default.
#
#   python3 -m venv .venv-nli
#   .venv-nli/bin/pip install -r requirements-nli.txt
#   .venv-nli/bin/python evaluate.py --backend all
#
# Versions below are what the measurements in README were taken with, on
# macOS, Python 3.9, CPU only.
torch==2.8.0
transformers==4.57.6
sentencepiece
protobuf
